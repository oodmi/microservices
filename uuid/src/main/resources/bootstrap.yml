spring:
  application:
    name: uuid
  cloud:
    config:
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
      discovery:
        service-id: config
        enabled: true